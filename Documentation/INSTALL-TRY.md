# Installing the dotnet-try global command #

1) Open a terminal and run the following command
``` bash
dotnet tool install --global dotnet-try
```

2) Once this is done, you can **navigate to any code/project directory** and run this command:
``` bash
dotnet try
```

3) [Try the examples](TRY.md)